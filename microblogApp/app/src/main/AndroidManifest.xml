<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thweibo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/microblog"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/microblog"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:largeHeap="true"
        <activity android:name=".DetailActivity" />
        <activity android:name=".AddBlogActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".OtherMeActivity" />
        <activity android:name=".BlackListActivity" />
        <activity android:name=".DirectMailActivity" />
        <activity android:name=".DMListActivity" />
        <activity android:name=".ReadDMActivity"/>
        <activity
            android:name=".RegisterActivity"
            android:label="Sign up" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>